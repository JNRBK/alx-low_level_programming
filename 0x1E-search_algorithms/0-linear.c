#include "search_algos.h"

int linear_search(int *array, size_t size, int value)
{
    size_t x;

    if (!array || size == 0)
        return (-1);
    
    for (x = 0; x < size; x++)
    {
        
    }
}